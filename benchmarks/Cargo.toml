[package]
name = "benchmarks"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1.4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.149"
rand = "0.10.0"
chrono = "0.4"

[dependencies.uuid]
version = "1.23.0"
# Lets you generate random UUIDs
features = [
    "v4",
]