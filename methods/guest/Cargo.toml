[package]
name = "eval_ast"
version = "0.1.0"
edition = "2021"

[dependencies]
risc0-zkvm = { version = "3", default-features = false }
dnf_core = { path = "../../crates/dnf_core", default-features = false }
