//! Host-side support for the benchmark driver (`main.rs`): the DSL parser
//! ([`parser`]) and the raw-batch event-index scanner ([`predata_processor`]).

pub mod parser;
pub mod predata_processor;
